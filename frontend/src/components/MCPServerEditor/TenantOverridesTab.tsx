import React from 'react';
import type { MCPServer } from '../../types/mcpServer';

interface Props { server: MCPServer }
export default function TenantOverridesTab(_p: Props) { return <div style={{ color: 'var(--text-secondary)' }}>Tenant overrides — wired in Task 8.5</div>; }
