# Agent 叠加权限与子会话详情发布记录

2026-09-10 已完成应用切换。运行服务检查通过，发布后的在线观察继续进行。

## 发布代码

- 已上线源码：`c3d2a25b6ad6f40f17c2f7a78443eec46c001e00`。
- 上一源码：`5129490b5db442edaa5c6f746226f003458326ed`。
- backend/frontend 产品版本均为 `1.10.3`，没有升语义版本。
- 实现代码已进入公司 `company/main`；本记录为后续文档提交，不改变已上线镜像。
- backend、worker、connector 使用同一不可变镜像，frontend 来自同一源码 SHA。
  镜像摘要、运行配置、备份标识及原始日志保存在仓库外的受控发布证据目录。

## 用户确认后的执行范围

用户明确修正了初版计划的备份范围：**本地演练只导出生产表结构；生产在线热备
只覆盖受影响的表，备份期间保持生产服务运行。** 初次全量导出已取消，未完成文件
已删除，没有将生产业务数据导入本地演练。

最终在线热备一次性覆盖 `agents`、`agent_permissions`、`audit_logs` 和
`alembic_version`，使用一致性 custom-format 快照、5 秒锁等待上限，并验证目录和
校验和。没有备份工作区历史、Redis、对象存储或 CLI 数据；本次不修改这些数据格式。
该备份不是全库恢复方案，不构成生产数据恢复授权。

权限迁移从 `model_extra_headers` 转到 `additive_agent_permissions`。
旧写入进程退出且数据库旧连接为零后执行转换，随后通过一个 Compose 命令启动
四个候选应用角色。数据库、Redis、AIO 和无关服务保持运行；窗口内没有构建、
拉取镜像、`down`、`remove-orphans` 或依赖服务重启。

开始停止旧应用为 09:07:25 UTC；迁移于 09:08:17 UTC 完成；09:08:43 UTC
启动四个新角色；09:09:05 UTC 确认代理健康接口恢复。停止请求至验证恢复约
100 秒，不声明零停机。用户在切换期间要求立即启动，后续验证全部在线完成。
验收后原子更新正式 Compose，没有再次重启服务。

## 验证结果

- 实际候选镜像在隔离 Docker 环境通过 53 项相关后端测试；前端构建、PC/H5
  提前打开 queued 子会话详情和跨租户拒绝验证见已有实现证据。
- 生产表结构在隔离 PostgreSQL 中恢复为 91 张表，业务数据为空。
  添加三种合成旧权限数据后，旧版四角色启动通过；普通在线迁移被拒绝且原状态
  未变；候选显式转换和四角色启动通过，前后授权结果一致。
- 生产逐项核对 166 个 Agent 和 166 条权限归一化审计，零差异。
- 四个应用角色均运行，三个后端角色健康；代理健康接口返回 200，nginx 校验通过。
  PostgreSQL、Redis、AIO 健康；相关容器未出现异常重启。
- 已观察到真实用户的 API、历史读取及 WebSocket 重连成功。受保护的定时触发器
  已完成；会话恢复状态继续跟踪，不以健康接口代替无损恢复证明。
- 未创建生产测试身份、令牌或合成外发消息；未额外执行真实模型子会话 smoke。
- 一条飞书渠道报告无效 `app_id`，其配置自 2026-04-22 未变。本次保留配置，
  未直接证明该错误在切换前已发生。历史挂起触发器的恢复告警也已记录，未改写审计状态。

## 观察及后续边界

已启动仓库外的只读观察任务，逐分钟记录服务健康、HTTP 状态、重启次数、活动租约、
发送回执、数据库锁和长事务，计划保留至少 24 小时。记录提交时近距离观察尚未满
30 分钟，不能把观察启动写成观察通过；原始结果继续保存在受控证据目录。
自动采集不等于承诺会话结束后的持续人工值守。

转换后不得让旧权限解释器直接接管现有数据。异常优先使用同源不可变镜像向前修复；
生产数据恢复或 schema downgrade 仍须另行决策。没有创建 Git tag 或托管 Release。
临时表结构演练容器、数据卷和网络已清理，其他开发环境和未提交改动保留。
