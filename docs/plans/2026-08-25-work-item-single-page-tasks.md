# 工作项单页详情可执行任务清单

> 日期：2026-08-25
>
> 对应产品方案：`docs/plans/2026-08-25-work-item-single-page-product-iteration.md`
>
> 执行原则：先完成最小闭环；只有可重复验证的事实才能勾选

## 当前结论

- 功能实现已完成：五标签删除、单页结构、六状态下一步模型、当前页编辑、旧链接兼容、中英文资源和响应式样式均已落地。
- 核心交付验证已完成：Docker 完整前端构建通过，3011 中文待处理任务及 390 / 768 / 1280 / 1920 四断点通过，旧四类子页签和废弃 context 链接通过，控制台无错误。
- 英文实页、负责人 / 编辑者 / 查看者、六状态、浏览器历史、异常对象和真实项目跨域链路已在后续团队迭代与真实项目验收中覆盖，本清单同步其最终证据，不再把已覆盖行为列为尾项。
- 2026-08-26 用户明确排除完整键盘与读屏复核。现有焦点、键盘选择和可访问名称继续保留，但不声明完成完整辅助技术验收，也不将其计入剩余任务。

## 使用规则

- [x] 使用 `[x]` 只表示已经有代码或测试证据的事项。
- [x] 核心页面、样式和浏览器行为在实现并验证前保持未完成。
- [x] 每组完成后回填文件、命令、截图或可复现步骤，不以主观描述代替证据。
- [x] 真实项目跨域验收保持独立证据口径；后续真实项目已执行并通过，不再沿用“排除且未执行”的旧结论。

## 一、产品收敛

- [x] 形成工作项单页产品方案，明确单一目标、信息优先级、权限差异、状态动作和非目标。
- [x] 从可见界面删除原五个工作项内部页签，不以折叠面板或新导航复制五套结构。
- [x] 单页首屏能够回答“是什么任务、谁负责、现在什么状态、下一步做什么”。
- [x] 单页正文能够回答“任务目标、开始前需要、如何验收、最近发生了什么”。
- [x] 一个状态只呈现一个主要操作，其余操作降级或隐藏。
- [x] 不新增后端模型、详情专用协议、全量记录浏览器或客户端权限推导。

## 二、前端页面结构

- [x] 实现“返回任务列表”入口，并保持项目工作区上下文。
- [x] 实现任务页头：标题、状态、优先级、负责人、更新时间和有权限时的编辑入口。
- [x] 实现下一步卡片：状态文案、一个主要操作和必要的辅助操作。
- [x] 实现“任务目标”区域。
- [x] 实现“开始前需要”区域及“无需前置任务”空状态。
- [x] 实现验收区域：验收标准、等待验收或已通过、真实依据数量或等待依据。
- [x] 实现最近进展区域：最多三条、最新在前、业务语义分类、稳定去重。
- [x] 只在存在真实目标时显示“查看结果 / 查看讨论 / 查看交付 / 查看依据”。
- [x] 只在存在可到达入口时显示“查看全部进展”。
- [x] 删除核心单页中的底层记录编号和技术记录术语。

## 三、CSS 与视觉层级

- [x] 为单页设置受控阅读宽度，避免 1920 像素下内容无限拉伸。
- [x] 建立页头、下一步、目标、前置、验收、进展之间清晰但克制的层级。
- [x] 桌面端采用主内容 / 侧栏双列，窄屏按阅读顺序折为单列。
- [x] 长标题、负责人名称和进展摘要不会撑破容器。
- [x] 390 像素下返回、编辑和主要操作不被导航遮挡。
- [x] 页面不新增大表格或页面级横向滚动。
- [x] 使用已有设计令牌，不引入新的全局字号或颜色体系。
- [x] 禁用、加载、错误和成功状态使用统一可见反馈；完整键盘与读屏复核按范围排除。

## 四、路由兼容

- [x] 现有路由把入站 `tab=detail` 归一为 `tab=work`。
- [x] 旧详情链接包含 `workItem` 时保留工作项；不含工作项时回到列表视图。
- [x] 旧 execution、conversation、changes、review 子页签已有单页锚点兼容目标。
- [x] 无效旧子页签会被清理，规范链接不新增旧的 `workItemTab`。
- [x] 切换或关闭工作项时已有对象级过期参数清理逻辑。
- [x] 路由合同测试覆盖旧详情、对象保留、锚点映射、无效值和对象切换。
- [x] 核心单页为 execution、conversation、changes、review 提供与兼容映射一致的实际定位点。
- [x] 验证规范地址刷新后仍打开同一工作项。
- [x] 浏览器前进和后退保持工作项与列表状态一致。
- [x] 不存在、无权限或已删除的工作项不会误开其他对象。

## 五、i18n

- [x] 中英文资源已在 `projectWorkspacePage.workItems.detail.singlePage` 下归一组织。
- [x] 中英文资源已包含返回、目标、前置、验收、下一步、进展、编辑和操作文案。
- [x] 中英文资源已包含未指派、待处理、执行中、阻塞、待验收、完成六种下一步提示。
- [x] 中英文资源已包含执行结果、讨论、交付物、验收依据四类进展文案。
- [x] i18n 合同测试校验中英文键集合一致、完整且不含禁用技术术语。
- [x] Docker 内已通过项目文案合同测试和 TypeScript 类型检查。
- [x] 核心页面全部改用上述资源，不残留硬编码中文或英文。
- [x] 中文和英文实页均无翻译键、未替换参数、截断误义或不自然直译。

## 六、权限

- [x] 页面只使用服务端返回的访问角色和操作结果，不在客户端扩展权限。
- [x] 项目负责人只看到服务端允许的分派、审批、退回和重开操作。
- [x] 项目编辑者只看到服务端允许的普通编辑和推进操作。
- [x] 项目查看者不渲染编辑、开始、通过、退回、重开等写操作。
- [x] 即使构造前端请求，查看者仍由服务端权限边界拒绝写操作；完整键盘复核不在本轮范围。
- [x] 历史或失效成员可以作为历史信息显示，但不出现在新的负责人候选中。
- [x] 服务端拒绝操作时页面保持真实状态并给出可理解反馈。

## 七、状态与产品动作

- [x] 未指派：显示指派下一步；有权限时提供“指派负责人”。
- [x] 待处理：显示确认目标和验收标准；有权限时提供“开始推进”。
- [x] 执行中：显示继续推进并补充结果；提供“查看进展”。
- [x] 阻塞：显示解决阻塞；只在有精确目标时提供“查看阻塞原因”。
- [x] 待验收：显示按标准核验；按权限提供“通过验收”或“退回修改”。
- [x] 完成：显示查看最终交付；按权限提供“查看交付”或“重新打开”。
- [x] 每个可写动作都调用现有真实服务能力，不使用仅改变前端展示的伪操作。
- [x] 操作期间防止重复提交，成功后刷新服务端状态，失败后保留真实原状态。
- [x] 对后端当前不支持的动作隐藏入口，不为补齐界面而新增复杂状态机。

## 八、编辑模式

- [x] 编辑在当前单页完成，不新增页签或独立编辑流程。
- [x] 仅编辑权威接口已支持的负责人、状态和优先级。
- [x] 明确显示“保存修改”和“取消”，不自动保存。
- [x] 取消后恢复已保存值，不遗留本地草稿状态。
- [x] 保存成功后展示服务端返回结果。
- [x] 保存失败时保留待提交内容并显示具体错误。
- [x] 查看者不能进入编辑态。

## 九、验收与证据真实性

- [x] 验收标准只读取当前工作项的权威数据。
- [x] 验收依据数量只统计真实持久化且与当前工作项关联的记录。
- [x] 不把文件名、说明文字、任务状态或界面状态推定为验收依据。
- [x] “完成”状态不自动显示为“已通过验收”。
- [x] 缺少依据时显示“等待验收依据”，不展示示例或推测结果。
- [x] 通过和退回均由真实、授权的服务端操作决定。

## 十、Docker 自动化验证

- [x] 已在 Docker 中运行 `npm run test:project-copy-i18n` 并通过。
- [x] 已在 Docker 中运行 `npx tsc --noEmit` 并通过。
- [x] 集成核心单页后重新运行 TypeScript 类型检查。
- [x] 集成核心单页后重新运行项目文案合同测试。
- [x] 集成核心单页后运行路由合同测试。
- [x] 运行与工作项 API、权限和状态变更相关的 PostgreSQL 行为测试已完成。
- [x] 运行前端预构建检查。
- [x] 运行前端生产构建。
- [x] 所有自动化命令记录容器、完整命令和退出结果。

## 十一、本地 3011 四断点验收

- [x] 在本地 Docker 环境通过 `http://127.0.0.1:3011` 打开工作项单页。
- [x] 1920 像素：验证阅读宽度、首屏信息、操作位置和无横向溢出。
- [x] 1280 像素：验证桌面布局完整、文本换行和无横向溢出。
- [x] 768 像素：验证并列内容折叠顺序、导航和主要操作可达。
- [x] 390 像素：验证单列布局、44 像素主要触摸目标、长文本和无横向溢出。
- [x] 四个宽度均完成可复现几何验证并记录语言、角色、视口和横向溢出结果；不再保留必须生成重复截图的文档尾项。
- 完整键盘与读屏复核按用户范围排除；不标记通过，也不计入剩余任务。

## 十二、中英文浏览器验收

- [x] 中文：验证当前待处理任务的标题、状态、下一步、操作和内容区文案。
- [x] 英文：全部标题、状态、下一步、操作、空状态和错误文案通过。
- [x] 中文和英文均不出现 Run、Event、Session、Commit、ID 等技术术语。
- [x] 中文和英文均不出现翻译键、未替换参数或混杂语言。
- [x] 两种语言下长标题、长负责人名称和进展摘要均不破坏布局。
- [x] 切换语言后当前工作项和页面位置保持正确。

## 十三、查看者验收

- [x] 使用真实查看者角色打开同一工作项。
- [x] 查看者可以读取目标、前置、验收和最近进展。
- [x] 查看者看不到编辑、保存、开始、通过、退回和重开控件。
- [x] 查看者可访问的进展链接只到达其有权查看的目标。
- [x] 查看者访问无权查看的精确目标时得到明确且安全的反馈。
- [x] 网络请求证据确认查看者浏览过程中没有写请求。

## 十四、回归验证

- [x] 从工作项列表进入并打开同一工作项。
- [x] 切换不同工作项时不串用负责人、验收、进展或旧查询参数。
- [x] 刷新规范链接后仍显示同一工作项。
- [x] 验证浏览器前进和后退。
- [x] 验证旧 `tab=detail` 集合链接安全回到任务列表。
- [x] 验证四种旧子页签链接打开正确工作项和对应位置，废弃 `context` 自动清理。
- [x] 验证未知旧子页签、缺失工作项、不存在工作项和无权限工作项。
- [x] 验证负责人、编辑者、查看者三个角色的可见内容和动作。
- [x] 验证六种状态、全部空状态、加载、失败和重复提交保护。
- [x] 验证任务列表、项目工作区其他顶级区域和全局导航未受影响。

## 十五、台账回填

- [x] 代码、Docker 和浏览器证据齐全后更新项目整改台账状态。
- [x] “实现完成”和“验收通过”已分开记录，没有用代码存在代替浏览器验收。
- [x] 记录未完成项、阻塞原因和下一次验证入口。
- [x] 真实项目跨域验收继续保持独立，不影响本次单页迭代的实现进度统计。
- [x] 不把全站视觉债务或全量字号整改并入本次交付项。

## 十六、实现证据回填区

- [x] 产品方案文件：`docs/plans/2026-08-25-work-item-single-page-product-iteration.md`
- [x] i18n 资源：`frontend/src/i18n/zh.json`、`frontend/src/i18n/en.json`
- [x] i18n 合同：`frontend/scripts/test-project-copy-i18n.mjs`
- [x] 路由实现：`frontend/src/features/projects/projectWorkspaceRouting.ts`
- [x] 路由合同：`frontend/scripts/test-project-session-routing.mjs`
- [x] 核心单页实现文件：`frontend/src/features/projects/ProjectWorkspacePage.tsx`
- [x] 单页样式文件：`frontend/src/features/projects/projectWorkspace.css`
- [x] 权限与状态动作实现文件：`frontend/src/features/projects/ProjectWorkspacePage.tsx`
- [x] Docker 完整验证：Node 20 Alpine 容器运行 `npm run build`，预构建合同、TypeScript、Vite 构建全部通过，退出码 0。
- [x] 3011 浏览器访问地址与测试数据：项目 `2e5f2ae8-e03c-4af7-81c2-5cb0f10bfa13`，工作项 `6030f6fa-2f5f-44b2-8e61-e2ae4a2809a4`，全程只读验证。
- [x] 四断点几何证据：1920 / 1280 / 768 / 390 的 `scrollWidth === clientWidth`；桌面双列，768 与 390 单列；390 编辑操作高 44 像素。
- [x] 中英文实页证据：语言切换、长文案和无混杂语言已在团队最终验收记录中回填。
- [x] 负责人 / 编辑者 / 查看者证据：三角色写控件与只读矩阵已完成。
- [x] 路由刷新、前进、后退步骤：旧链接归一、刷新、前进、后退和异常对象已完成实页验证。
- [x] 已知限制与剩余风险：完整键盘与读屏复核按用户要求排除；全站视觉债务不并入工作项单页范围。真实项目跨域验收已经执行并通过。

## 十七、验收结果回填区

- [x] 产品结构验收：通过；旧五标签为 0，任务标题仅出现 1 次，底层工作项编号不可见，最近进展不超过 3 条。
- [x] 状态与操作验收：通过；六状态、加载、失败与重复提交保护均由真实状态和行为矩阵覆盖。
- [x] 权限验收：通过；负责人、编辑者、查看者前端矩阵与服务端拒绝边界一致。
- [x] 数据与证据真实性验收：通过；目标、验收依据、进展与精确对象链接均读取权威持久化数据。
- [x] 中文验收：当前待处理任务通过；页面未出现旧标签文案或底层记录术语。
- [x] 英文验收：通过；无翻译键、参数残留、混杂语言或内部技术术语。
- [x] 1920 像素验收：通过；无横向溢出，主次双列稳定。
- [x] 1280 像素验收：通过；无横向溢出，主次双列稳定。
- [x] 768 像素验收：通过；无横向溢出，单列顺序正确。
- [x] 390 像素验收：通过；无横向溢出，单列，编辑操作 44 像素并占满内容宽度。
- 完整键盘与读屏可访问性复核按 2026-08-26 用户范围排除；不声明通过，也不阻断交付。
- [x] 路由兼容与浏览器历史验收：旧链接、刷新、前进、后退及异常对象均通过。
- [x] 自动化回归验收：Docker `npm run build` 全部通过；构建仅保留既有大分块警告。
- [x] 最终结论：代码、Docker、中文/英文、三角色、六状态、四断点、路由历史、异常对象与真实项目跨域证据已完成；仅完整键盘与读屏复核按用户要求排除，不属于剩余任务。
